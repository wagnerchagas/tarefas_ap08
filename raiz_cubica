#programas raiz cubica
import random

n = random.randint(1, 100)

raiz_cubica = n ** (1/3)

raiz_quadrada = (n + 1) ** 0.5

print("Numero gerado:", n)
print("Raiz cúbica:", round(raiz_cubica, 2))
print("Raiz quadrada:", round(raiz_quadrada, 2))